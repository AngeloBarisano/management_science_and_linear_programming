from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='this is a course for university set up in the geneal venv form',
    author='Angelo Barisano',
    license='',
)
